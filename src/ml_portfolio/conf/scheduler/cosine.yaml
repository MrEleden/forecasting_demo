# Cosine Annealing LR scheduler configuration (PyTorch)
# Anneals learning rate using cosine function

_target_: torch.optim.lr_scheduler.CosineAnnealingLR

T_max: 50  # Maximum number of iterations
eta_min: 0.0  # Minimum learning rate
